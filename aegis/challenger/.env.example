# challenger 独立配置（本目录拷到队友机器后：cp .env.example .env 并填写）
CHALLENGER_PK=

# Monad testnet（默认官方 RPC + Ankr 备用，一般不用改）
MONAD_TESTNET_RPC=https://testnet-rpc.monad.xyz

# 链上合约（与 proposer 侧一致；v4 部署）
REGISTRY=0x4622D041696942dC873a8A5E54f1e1ca9669c90B
VALIDATION=0x8b96a09eb50409FE4c402cB9Bb9D1Ef79bbe0cEa
VAULT=0x07Be2FCdAA649F11177AaCCbd68A5bFF36aB65bc

# proposer 侧 orchestrator 地址（拉决策原文用；同网段直连或隧道）
ORCH_URL=http://localhost:8787

AGENT_ID=1
POLL_MS=4000
# 拒绝也上链留证（demo 时改 true）
RECORD_REJECT=false

# ---- L5 交叉模型层（可选；默认关闭 = 与 Phase 2 行为完全一致）----
# 开启后会另起一个**不同家族**的模型独立重推导 proposer 的决策，分歧则 response=0。
# MODEL_CHALLENGE=true
# LLM_BASE_URL=https://your-endpoint/v1     # challenger 自己能访问到的模型端点
# LLM_API_KEY=
# LLM_MODEL=qwen2.5-7b-instruct             # 与 proposer 不同家族；temperature 强制 0
# LLM_CHALLENGER_MODEL=qwen2.5-7b-instruct
